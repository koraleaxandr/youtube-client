import { ColorOnDateDirective } from './color-on-date.directive';

describe('ColorOnDateDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorOnDateDirective();
    expect(directive).toBeTruthy();
  });
});
