import { BoxShadowColorizingDirective } from './box-shadow-colorizing.directive';

describe('BoxShadowColorizingDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxShadowColorizingDirective();
    expect(directive).toBeTruthy();
  });
});
